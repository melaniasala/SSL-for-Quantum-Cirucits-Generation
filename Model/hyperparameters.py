HYPERPARAMETERS = {
    'model': {
        'window': 4, # corresponds to the max sequence length allowed (number of temporal steps the model can look back)
        # 'input_size': 10,
        # 'hidden_size': 128,
        # 'output_size': 10
    },
    'training': {
        'lr': 0.001,
        'epochs': 10
    }
}
